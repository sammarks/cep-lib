import * as csInterface from './csinterface'
import * as cep from './cep'

console.warn('You should only consume CEP-lib by importing from filepaths directly.')

export { csInterface, cep }
export default { csInterface, cep }
